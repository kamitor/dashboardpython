<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
    "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  macOS entitlements for PACKAGE_NAME.
  Applied during code signing:
    codesign --entitlements packaging/macos/entitlements.plist ...

  TODO: Enable only the entitlements your app actually requires.
  Requesting unnecessary entitlements delays notarization review.

  Hardened runtime is REQUIRED for notarization as of macOS 10.15+.
-->
<plist version="1.0">
<dict>
    <!-- Hardened runtime — mandatory for notarization -->
    <key>com.apple.security.cs.hardened-runtime</key>
    <true/>

    <!-- Allow JIT compilation (needed by some Python extensions) -->
    <!--
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    -->

    <!-- Allow unsigned executable memory (needed by some C extensions) -->
    <!--
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    -->

    <!-- Disable library validation (needed if loading 3rd-party .dylibs) -->
    <!--
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    -->

    <!-- Network access -->
    <!--
    <key>com.apple.security.network.client</key>
    <true/>
    -->

    <!-- File access — remove if not needed -->
    <!--
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    -->
</dict>
</plist>
